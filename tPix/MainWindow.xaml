<Window x:Class="tPix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conveters="clr-namespace:tPix.Converters"
        xmlns:local="clr-namespace:tPix" xmlns:views="clr-namespace:tPix.Views"
        mc:Ignorable="d"
        Title="{Binding Path=ImageDescription}" Height="700" Width="1224" Background="Pink">

    <Grid Background="{StaticResource StdBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <views:SelectionPane Grid.Row="0"/>
            <views:ListPane Grid.Row="1"/>
            <views:ButtonsPane Grid.Row="2"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <views:FiltersPane Grid.Row="0"/>
            <views:ImagePane Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
